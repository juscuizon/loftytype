mixin navigation-links
	ul
		li: a.text(href="#") SERVICES
		li: a.text(href="#") ABOUT US
		li: a.non-text(href="#"): img.logo(src="{{site.baseurl}}/assets/img/logo.svg")
		li: a.text(href="#") BLOG
		li: a.text(href="#") CONTACT US

.welcome-section-container

	.navigation
		+navigation-links

	.content-area
		.title We Design, Develop, and Realize your #[span.colored-text Ideas.]